# Copy to .env and use when running the app against docker/docker-compose services.
# From project root: export $(grep -v '^#' docker/.env.example | xargs) && go run ./cmd

# Database (matches docker-compose postgres)
DATABASE_URL=postgres://app:appsecret@localhost:5432/million_rps?sslmode=disable

# Redis (matches docker-compose redis)
REDIS_URL=redis://localhost:6379/0

# Kafka (matches docker-compose kafka)
KAFKA_BROKERS=localhost:9092

# Required for auth endpoints
JWT_SECRET=your-jwt-secret-change-in-production

# Optional
# HTTP_PORT=8080
# CACHE_TTL_SEC=300
# KAFKA_TODO_TOPIC=todo-commands
